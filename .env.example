## Copy to .env (gitignored) and fill in a real value before running
## `docker compose up`. This is a local-dev-only password for the Service
## Bus emulator's required sqledge companion (ADR-0013) — it holds no
## Lakbay data of its own. Never reused for any real/deployed environment.
DB_PASSWORD=ChangeMe_Local_Dev_Only_1!
